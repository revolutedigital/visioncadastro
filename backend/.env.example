# =============================================================================
# BACKEND ENVIRONMENT VARIABLES - Railway Production Template
# =============================================================================
# Copie este arquivo para .env e preencha com os valores reais
# NUNCA commite o arquivo .env com secrets reais!
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Railway injeta automaticamente quando você conecta PostgreSQL)
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://user:password@host:5432/database

# -----------------------------------------------------------------------------
# REDIS (Railway injeta automaticamente quando você conecta Redis)
# -----------------------------------------------------------------------------
REDIS_URL=redis://default:password@host:6379
REDIS_HOST=host
REDIS_PORT=6379
REDIS_PASSWORD=password

# -----------------------------------------------------------------------------
# 🔴 CRITICAL - API KEYS (Configure manualmente no Railway)
# -----------------------------------------------------------------------------

# Google Maps API (Geocoding + Places + Photos)
# Obtenha em: https://console.cloud.google.com/
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Anthropic Claude API (Vision AI + Tipologia)
# Obtenha em: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API (Opcional - Fallback)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here_or_leave_empty

# CNPJA API (Consulta CNPJ - Receita, Simples, CCC, QSA)
# Obtenha em: https://cnpja.com
CNPJA_API_KEY=your_cnpja_api_key_here

# SERPRO CPF API (Consulta CPF - OAuth2)
# Obtenha em: https://servicos.serpro.gov.br/
SERPRO_CPF_CONSUMER_KEY=your_serpro_consumer_key
SERPRO_CPF_CONSUMER_SECRET=your_serpro_consumer_secret
SERPRO_CPF_TOKEN_URL=https://gateway.apiserpro.serpro.gov.br/token
SERPRO_CPF_API_URL=https://gateway.apiserpro.serpro.gov.br/consulta-cpf-df/v1/cpf

# -----------------------------------------------------------------------------
# SECURITY
# -----------------------------------------------------------------------------

# JWT Secret para autenticação (gerar random string segura)
# Gere com: openssl rand -base64 32
JWT_SECRET=your_super_secure_random_string_here_minimum_32_chars

# -----------------------------------------------------------------------------
# APPLICATION CONFIG
# -----------------------------------------------------------------------------

# Environment
NODE_ENV=production

# Server Port (Railway define automaticamente via $PORT)
PORT=4000

# Modelo Claude para Vision AI
# Options: "haiku" (73% mais barato) ou "sonnet" (máxima qualidade)
CLAUDE_VISION_MODEL=haiku

# Feature flag: usar CNPJA em vez de ReceitaWS (default: true)
USE_CNPJA=true

# Diretório de uploads de fotos
# Railway: /app/uploads/fotos (com Volume montado)
# Local: caminho absoluto
PHOTOS_DIR=/app/uploads/fotos

# -----------------------------------------------------------------------------
# FRONTEND URL (Para CORS)
# -----------------------------------------------------------------------------

# URL do frontend em produção (Railway)
FRONTEND_URL=https://scampepisico-frontend.up.railway.app

# -----------------------------------------------------------------------------
# OPTIONAL - Monitoring e Analytics
# -----------------------------------------------------------------------------

# Sentry DSN (Error tracking)
# SENTRY_DSN=https://your-sentry-dsn-here

# Log Level
# LOG_LEVEL=info
